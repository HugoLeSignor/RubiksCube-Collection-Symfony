<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }
        .header {
            background: #e74c3c;
            color: white;
            padding: 20px;
            text-align: center;
            border-radius: 10px 10px 0 0;
        }
        .content {
            background: #f9f9f9;
            padding: 30px;
            border-radius: 0 0 10px 10px;
        }
        .info-box {
            background: white;
            padding: 15px;
            margin: 10px 0;
            border-left: 4px solid #e74c3c;
        }
        .message-box {
            background: #fff;
            padding: 20px;
            border: 1px solid #ddd;
            border-radius: 5px;
            margin: 20px 0;
        }
        .action-btn {
            display: inline-block;
            background: #e74c3c;
            color: white;
            padding: 12px 30px;
            text-decoration: none;
            border-radius: 5px;
            margin: 20px 0;
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>🔔 Nouveau Message de Contact</h1>
    </div>
    <div class="content">
        <h2>Nouveau message reçu</h2>
        
        <div class="info-box">
            <strong>De :</strong> {{ name }}<br>
            <strong>Email :</strong> <a href="mailto:{{ email }}">{{ email }}</a><br>
            <strong>Sujet :</strong> {{ subject }}<br>
            <strong>Date :</strong> {{ "now"|date("d/m/Y à H:i") }}
        </div>
        
        <div class="message-box">
            <h3>Message :</h3>
            <p>{{ message|nl2br }}</p>
        </div>
        
        <p style="text-align: center;">
            <a href="{{ url('admin', {routeName: 'Contact', action: 'detail', entityId: contactId}) }}" class="action-btn">
                Voir dans EasyAdmin
            </a>
        </p>
        
        <p><strong>Action requise :</strong> Répondre au message via l'interface d'administration.</p>
    </div>
</body>
</html>
